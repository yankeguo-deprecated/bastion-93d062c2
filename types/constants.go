package types

import "ireul.com/web"

const (
	// DEV == "development"
	DEV = web.DEV
	// PROD == "production"
	PROD = web.PROD
	// TEST == "test"
	TEST = web.TEST
)

// AccountPrefix prefix of remote account
const AccountPrefix = "bastion-"
